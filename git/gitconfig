[user]
	name = Dániel Arató
	email = nil.the.human@gmail.com
[core]
	editor = vim
[alias]
	a = add
	ap = add -p
	b = branch
	cm = commit
	ca = commit --amend
	co = checkout
	d = diff --minimal --word-diff
	ds = diff --minimal --word-diff --staged
    h = log -n 20 --graph --oneline --decorate=short
	l = log -n 20 --graph --oneline --decorate=short
	ls = log --summary
    rev = revert --no-commit
	s = status -sb
[log]
	date = short
	decorate = short
	showroot = true
[diff]
	algorithm = patience
[color "decorate"]
	HEAD = red
[credential]
	helper = cache
[push]
	default = simple
